CHOST=aarch64-unknown-linux-gnu
CBUILD=x86_64-pc-linux-gnu

ROOT=/usr/${CHOST}/

ACCEPT_KEYWORDS="arm64"

FEATURES="-collision-protect -ipc-sandbox -network-sandbox -pid-sandbox getbinpkg noman noinfo nodoc"

PKGDIR="/packages"
PORTAGE_TMPDIR=${ROOT}tmp/

# dev-libs/glib-2.84.4 does not compile
USE="-introspection"
